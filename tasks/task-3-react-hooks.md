# Задание 3: Реализация кастомных хуков

Привет!

В этом задании мы сосредоточимся на реализации кастомных хуков, которые часто встречаются в реальных проектах и упоминаются в вакансии.

## Шаги:

1.  **Создай папку `hooks`:**
    *   В корне проекта создай папку `src/hooks` (или просто `hooks`, если ты предпочитаешь более плоскую структуру).

2.  **Реализуй `usePrevious`:**
    *   В папке `src/hooks` создай файл `usePrevious.ts`.
    *   Внутри этого файла реализуй кастомный хук `usePrevious`, который будет возвращать предыдущее значение переданной ему переменной.
    *   **Подсказка:** Используй `useRef`.
    *   **Шаблон для `usePrevious.ts`:**
        ```typescript
        import { useRef, useEffect } from 'react';

        export function usePrevious<T>(value: T): T | undefined {
          // TODO: Реализуй логику хука usePrevious
          // Подсказка: используй useRef для хранения предыдущего значения
          // и useEffect для обновления этого значения после каждого рендера.
        }
        ```

3.  **Реализуй `useDebounce`:**
    *   В папке `src/hooks` создай файл `useDebounce.ts`.
    *   Внутри этого файла реализуй кастомный хук `useDebounce`, который будет откладывать выполнение функции или обновление значения до тех пор, пока не пройдет определенное время без новых вызовов.
    *   **Подсказка:** Используй `useState`, `useEffect` и `setTimeout`/`clearTimeout`.
    *   **Шаблон для `useDebounce.ts`:**
        ```typescript
        import { useState, useEffect } from 'react';

        export function useDebounce<T>(value: T, delay: number): T {
          // TODO: Реализуй логику хука useDebounce
          // Подсказка: используй useState для хранения отложенного значения
          // и useEffect с setTimeout/clearTimeout для управления задержкой.
        }
        ```

4.  **Реализуй `useThrottle`:**
    *   В папке `src/hooks` создай файл `useThrottle.ts`.
    *   Внутри этого файла реализуй кастомный хук `useThrottle`, который будет ограничивать частоту вызовов функции, позволяя ей выполняться не чаще, чем раз в определенный интервал времени.
    *   **Подсказка:** Используй `useRef`, `useState` (опционально) и `setTimeout`/`clearTimeout`.
    *   **Шаблон для `useThrottle.ts`:**
        ```typescript
        import { useState, useEffect, useRef } from 'react';

        export function useThrottle<T>(value: T, interval: number): T {
          // TODO: Реализуй логику хука useThrottle
          // Подсказка: используй useRef для хранения последнего значения и таймера.
          // useEffect будет отвечать за установку и очистку таймера.
        }
        ```

5.  **Протестируй хуки на главной странице:**
    *   Открой `pages/index.tsx`.
    *   Импортируй реализованные хуки.
    *   Создай простые примеры использования каждого хука, чтобы убедиться в их корректной работе. Например, для `usePrevious` можно отображать текущее и предыдущее значение счетчика, для `useDebounce` - задержку ввода текста в инпут, для `useThrottle` - ограничение вызовов функции при скролле или изменении размера окна.

Удачи!